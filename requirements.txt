# Qwen-Image-Edit-2511 + Lightning LoRA Requirements
# Base Image: vastai/base-image:cuda-12.4.1-cudnn-devel-ubuntu22.04-py310-ipv2

# Core ML Frameworks - PyTorch 2.6.0 with CUDA 12.4 support
--extra-index-url https://download.pytorch.org/whl/cu124
torch==2.6.0
torchvision==0.21.0
torchaudio==2.6.0

# Hugging Face Libraries
# NOTE: diffusers is installed from git in setup.sh for QwenImageEditPlusPipeline support
# pip install git+https://github.com/huggingface/diffusers
transformers>=4.51.3
accelerate>=0.30.0
safetensors>=0.4.0
peft>=0.10.0  # Required for LoRA loading
sdnq  # SDNQ 4-bit quantization support
# dfloat11[cuda12]  # DFloat11 lossless compression (install separately with: pip install -U dfloat11[cuda12])
# Image Processing
Pillow>=10.0.0
opencv-python>=4.8.0

# Utilities
numpy>=1.24.0
tqdm>=4.66.0
einops>=0.7.0
requests>=2.31.0

# xformers for PyTorch 2.6.0
xformers==0.0.29.post3

# Sentencepiece for tokenizers
sentencepiece>=0.2.0

# For video processing (optional)
imageio>=2.31.0
imageio-ffmpeg>=0.4.9
